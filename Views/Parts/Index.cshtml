@model IEnumerable<Repairshop.Models.AmountPartsInStorage>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Part.Brand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Part.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Part.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AmountInStorage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Part.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.AmountInStorage < 1)
        {


            <tr style="background-color:firebrick">
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AmountInStorage)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Price)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
        else
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AmountInStorage)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Part.Price)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    }

</table>
